{
  "name": "testclassexample",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "cypress": "npm cache clean --force && cypress open  --config watchForFileChanges=true",
    "test": "cypress run --spec cypress/integration/Test/**/* --browser chrome"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "chai": "^4.2.0"
  },
  "devDependencies": {
    "cypress": "^4.12.1"
  }
}
